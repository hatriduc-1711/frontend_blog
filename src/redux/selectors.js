export const registerSelector = (state) => state.register.message;
export const loginSelector = (state) => state.login.message;

// user
export const createPostsSelector = (state) => state.createPosts.message;
export const allPostsSelector = (state) => state.allPosts.response;
export const postsSelector = (state) => state.posts.posts;
